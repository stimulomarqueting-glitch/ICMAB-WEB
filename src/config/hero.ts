/**
 * Home hero variant.
 *   'field' → HeroField: animated sublimation field on the plum backdrop
 *             (particles rise from the source, condense on the substrate).
 *   'photo' → Hero: the full-bleed duotone product photo.
 * Both read the same copy from home.hero; switch here to compare.
 */
export const heroVariant: 'field' | 'photo' = 'field';
