---
import { brand } from '../config/brand';
import { leadCapture, isMisconfigured } from '../config/leadCapture';
import { getContent, type Locale } from '../content';
import Icon from './Icon.astro';
import SectionHeader from './SectionHeader.astro';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const t = getContent(locale).waitlist;
const f = t.form;

// Surface misconfiguration early: visible in `astro dev` / `astro build` logs.
if (isMisconfigured) {
  console.warn(
    '\n[leadCapture] ⚠ mode is "production" but formEndpoint is empty.\n' +
      '            The form will fall back to demo behaviour (no data sent).\n' +
      '            Set formEndpoint in src/config/leadCapture.ts to go live.\n'
  );
}
---

<section class="section section--alt" id="waitlist" aria-labelledby="waitlist-title">
  <div class="container waitlist__wrap">
    <div class="waitlist__intro">
      <SectionHeader kicker={t.kicker} title={t.title} intro={t.intro} id="waitlist-title" />

      <ul class="waitlist__reasons reveal" role="list">
        {t.reasons.map((reason) => (
          <li>
            <span class="waitlist__reason-icon" aria-hidden="true"><Icon name="check" /></span>
            {reason}
          </li>
        ))}
      </ul>

      <p class="waitlist__hint reveal">{f.requiredHint}</p>

      {/* Lead magnet — hidden until a validated PDF exists.
          Enable in src/config/leadCapture.ts (leadMagnet.enabled + url). */}
      {leadCapture.leadMagnet.enabled && (
        <aside class="waitlist__magnet reveal">
          <h3>{t.leadMagnet.title}</h3>
          <p>{t.leadMagnet.text}</p>
          <a class="btn btn--ghost" href={leadCapture.leadMagnet.url} download data-cta-source="lead-magnet">
            {t.leadMagnet.button}
          </a>
        </aside>
      )}
    </div>

    <form
      class="waitlist__form reveal"
      id="waitlist-form"
      method="post"
      novalidate
      data-mode={leadCapture.mode}
      data-endpoint={leadCapture.formEndpoint}
      data-project={brand.projectName}
      data-language={locale}
    >
      <div class="waitlist__row">
        <div class="field">
          <label for="wl-fullname">{f.fullName.label} *</label>
          <input id="wl-fullname" name="fullName" type="text" autocomplete="name" required placeholder={f.fullName.placeholder} />
          <p class="field__error" id="wl-fullname-error" data-error-for="wl-fullname" hidden>{f.fullName.error}</p>
        </div>
        <div class="field">
          <label for="wl-email">{f.email.label} *</label>
          <input id="wl-email" name="email" type="email" autocomplete="email" required placeholder={f.email.placeholder} />
          <p class="field__error" id="wl-email-error" data-error-for="wl-email" hidden>{f.email.error}</p>
        </div>
      </div>

      <div class="waitlist__row">
        <div class="field">
          <label for="wl-org">{f.organization.label} *</label>
          <input id="wl-org" name="organization" type="text" autocomplete="organization" required placeholder={f.organization.placeholder} />
          <p class="field__error" id="wl-org-error" data-error-for="wl-org" hidden>{f.organization.error}</p>
        </div>
        <div class="field">
          <label for="wl-role">{f.role.label}</label>
          <input id="wl-role" name="role" type="text" autocomplete="organization-title" placeholder={f.role.placeholder} />
        </div>
      </div>

      <div class="waitlist__row">
        <div class="field">
          <label for="wl-country">{f.country.label}</label>
          <input id="wl-country" name="country" type="text" autocomplete="country-name" placeholder={f.country.placeholder} />
        </div>
        <div class="field">
          <label for="wl-orgtype">{f.organizationType.label} *</label>
          <select id="wl-orgtype" name="organizationType" required>
            <option value="" selected disabled>—</option>
            {f.organizationType.options.map((o) => (
              <option value={o.value}>{o.label}</option>
            ))}
          </select>
          <p class="field__error" id="wl-orgtype-error" data-error-for="wl-orgtype" hidden>{f.organizationType.error}</p>
        </div>
      </div>

      <div class="waitlist__row">
        <div class="field">
          <label for="wl-area">{f.areaOfInterest.label}</label>
          <select id="wl-area" name="areaOfInterest">
            <option value="" selected>—</option>
            {f.areaOfInterest.options.map((o) => (
              <option value={o.value}>{o.label}</option>
            ))}
          </select>
        </div>
        <div class="field">
          <label for="wl-need">{f.currentNeed.label} *</label>
          <select id="wl-need" name="currentNeed" required>
            <option value="" selected disabled>—</option>
            {f.currentNeed.options.map((o) => (
              <option value={o.value}>{o.label}</option>
            ))}
          </select>
          <p class="field__error" id="wl-need-error" data-error-for="wl-need" hidden>{f.currentNeed.error}</p>
        </div>
      </div>

      <div class="field">
        <label for="wl-timeline">{f.timeline.label}</label>
        <select id="wl-timeline" name="timeline">
          <option value="" selected>—</option>
          {f.timeline.options.map((o) => (
            <option value={o.value}>{o.label}</option>
          ))}
        </select>
      </div>

      <div class="field">
        <label for="wl-message">{f.message.label}</label>
        <textarea id="wl-message" name="message" rows="4" placeholder={f.message.placeholder}></textarea>
      </div>

      <div class="field field--consent">
        {/* TODO(client/legal): final consent wording pending legal review. */}
        <label class="consent">
          <input id="wl-consent" name="consent" type="checkbox" required />
          <span>
            {f.consent.labelBefore}
            {' '}<a href={leadCapture.privacyUrl} target="_blank" rel="noopener">{f.consent.privacyLabel}</a>{f.consent.labelAfter}
          </span>
        </label>
        <p class="field__error" id="wl-consent-error" data-error-for="wl-consent" hidden>{f.consent.error}</p>
      </div>

      <div class="waitlist__actions">
        <button class="btn btn--primary waitlist__submit" type="submit" data-label={f.submit} data-label-busy={f.submitting}>
          {f.submit}
        </button>
        <p class="waitlist__nospam">{f.noSpam}</p>
      </div>

      <p class="waitlist__status waitlist__status--error" data-status="error" role="alert" hidden>
        {f.errorText}
        {' '}<a href={`mailto:${leadCapture.contactEmail}`}>{leadCapture.contactEmail}</a>.
      </p>
    </form>

    <div class="waitlist__success" data-status="success" role="status" tabindex="-1" hidden>
      <span class="waitlist__success-icon" aria-hidden="true"><Icon name="check" /></span>
      <h3>{f.successTitle}</h3>
      <p>{f.successText}</p>
    </div>
  </div>
</section>

<style>
  .waitlist__wrap {
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    gap: clamp(2rem, 5vw, 4.5rem);
    align-items: start;
  }

  .waitlist__reasons {
    list-style: none;
    margin: 0 0 1.8rem;
    padding: 0;
    display: grid;
    gap: 0.7rem;
  }

  .waitlist__reasons li {
    display: flex;
    gap: 0.7rem;
    align-items: flex-start;
    color: var(--text-2);
    font-size: 0.96rem;
  }

  .waitlist__reason-icon {
    flex: 0 0 auto;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.35rem;
    height: 1.35rem;
    margin-top: 0.1rem;
    border-radius: 50%;
    background: rgba(47, 217, 195, 0.12);
    color: var(--accent);
  }

  .waitlist__reason-icon svg {
    width: 0.8rem;
    height: 0.8rem;
  }

  .waitlist__hint {
    font-size: 0.82rem;
    color: var(--text-3);
  }

  .waitlist__magnet {
    margin-top: 2rem;
    border: 1px dashed var(--line-strong);
    border-radius: var(--radius-m);
    padding: 1.4rem 1.6rem;
  }

  .waitlist__magnet h3 { font-size: 1.02rem; }
  .waitlist__magnet p { color: var(--text-2); font-size: 0.92rem; }

  .waitlist__form {
    background: var(--bg-raised);
    border: 1px solid var(--line);
    border-radius: var(--radius-l);
    padding: clamp(1.4rem, 3vw, 2.2rem);
    display: grid;
    gap: 1.15rem;
  }

  .waitlist__row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.15rem;
  }

  .field {
    display: grid;
    gap: 0.4rem;
    align-content: start;
  }

  .field label {
    font-size: 0.86rem;
    font-weight: 600;
    color: var(--text-2);
  }

  .field input,
  .field select,
  .field textarea {
    width: 100%;
    padding: 0.7rem 0.9rem;
    background: var(--bg);
    border: 1px solid var(--line-strong);
    border-radius: var(--radius-s);
    color: var(--text);
    font: inherit;
    font-size: 0.95rem;
    transition: border-color 0.15s var(--ease);
  }

  .field textarea { resize: vertical; }

  .field input::placeholder,
  .field textarea::placeholder {
    color: var(--text-3);
  }

  .field input:focus-visible,
  .field select:focus-visible,
  .field textarea:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 0;
    border-color: var(--accent);
  }

  .field input[aria-invalid='true'],
  .field select[aria-invalid='true'] {
    border-color: #ff7a7a;
  }

  .field__error {
    margin: 0;
    font-size: 0.8rem;
    color: #ff9d9d;
  }

  .consent {
    display: flex;
    gap: 0.65rem;
    align-items: flex-start;
    font-size: 0.85rem;
    color: var(--text-2);
    font-weight: 400 !important;
    cursor: pointer;
  }

  .consent input {
    width: 1.05rem;
    height: 1.05rem;
    margin-top: 0.2rem;
    accent-color: var(--accent);
    flex: 0 0 auto;
  }

  .waitlist__actions {
    display: flex;
    align-items: center;
    gap: 1.2rem;
    flex-wrap: wrap;
  }

  .waitlist__submit {
    min-width: 12rem;
  }

  .waitlist__submit[disabled] {
    opacity: 0.6;
    cursor: wait;
    transform: none;
  }

  .waitlist__nospam {
    margin: 0;
    font-size: 0.8rem;
    color: var(--text-3);
    max-width: 16rem;
  }

  .waitlist__status {
    margin: 0;
    font-size: 0.9rem;
  }

  .waitlist__status--error { color: #ff9d9d; }
  .waitlist__status--error a { color: #ffc4c4; }

  .waitlist__success {
    grid-column: 2;
    background:
      linear-gradient(120deg, rgba(47, 217, 195, 0.12), rgba(90, 169, 255, 0.06)),
      var(--bg-raised);
    border: 1px solid rgba(47, 217, 195, 0.4);
    border-radius: var(--radius-l);
    padding: 2.2rem;
  }

  .waitlist__success-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.6rem;
    height: 2.6rem;
    border-radius: 50%;
    background: rgba(47, 217, 195, 0.16);
    color: var(--accent);
    margin-bottom: 1rem;
  }

  .waitlist__success-icon svg {
    width: 1.3rem;
    height: 1.3rem;
  }

  .waitlist__success h3 { color: var(--accent-soft); }
  .waitlist__success p { color: var(--text-2); margin: 0; }

  @media (max-width: 56rem) {
    .waitlist__wrap { grid-template-columns: 1fr; }
    .waitlist__success { grid-column: 1; }
  }

  @media (max-width: 36rem) {
    .waitlist__row { grid-template-columns: 1fr; }
    .waitlist__submit { width: 100%; }
    .waitlist__nospam { max-width: none; }
  }
</style>

<script>
  /**
   * Waitlist form controller — see README "Lead capture setup".
   *
   * Modes (configured in src/config/leadCapture.ts, passed via data-mode):
   *   demo        → validate, log payload, simulate success. Nothing sent.
   *   production  → POST the JSON payload to data-endpoint.
   *                 If the endpoint is missing, warns and falls back to demo.
   *
   * The payload is a clean structured object (project, language, sourcePage,
   * ctaSource, qualification fields, utm, submittedAt) — ready for a CRM,
   * a webhook (Make/Zapier → Sheets/Airtable/HubSpot) or a custom API.
   */
  import { trackEvent, getCtaSource, getUtm } from '../scripts/leadTracking';

  const form = document.getElementById('waitlist-form') as HTMLFormElement | null;

  if (form) {
    const mode = form.dataset.mode ?? 'demo';
    const endpoint = form.dataset.endpoint ?? '';
    const isLive = mode === 'production' && endpoint !== '';

    if (mode === 'production' && !endpoint) {
      console.warn('[leadCapture] production mode without formEndpoint — falling back to demo behaviour.');
    }

    const submitBtn = form.querySelector<HTMLButtonElement>('.waitlist__submit')!;
    const errorBox = form.querySelector<HTMLElement>('[data-status="error"]')!;
    const successBox = document.querySelector<HTMLElement>('[data-status="success"]')!;

    const showFieldError = (input: HTMLElement, show: boolean) => {
      const msg = form.querySelector<HTMLElement>(`[data-error-for="${input.id}"]`);
      input.setAttribute('aria-invalid', String(show));
      if (msg) {
        msg.hidden = !show;
        input.setAttribute('aria-describedby', show ? msg.id : '');
      }
    };

    const validate = (): boolean => {
      let ok = true;
      let firstInvalid: HTMLElement | null = null;

      form.querySelectorAll<HTMLInputElement | HTMLSelectElement>('[required]').forEach((el) => {
        let valid = el.type === 'checkbox' ? (el as HTMLInputElement).checked : el.value.trim() !== '';
        if (valid && el.type === 'email') valid = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(el.value.trim());
        showFieldError(el, !valid);
        if (!valid) {
          ok = false;
          firstInvalid ??= el;
        }
      });

      firstInvalid?.focus();
      return ok;
    };

    /** Structured lead payload — keep in sync with README "Lead capture setup". */
    const buildPayload = () => {
      const data = new FormData(form);
      const v = (name: string) => String(data.get(name) ?? '').trim();
      return {
        project: form.dataset.project ?? '',
        language: form.dataset.language ?? '',
        sourcePage: window.location.pathname,
        ctaSource: getCtaSource(),
        fullName: v('fullName'),
        email: v('email'),
        organization: v('organization'),
        role: v('role'),
        country: v('country'),
        organizationType: v('organizationType'),
        areaOfInterest: v('areaOfInterest'),
        currentNeed: v('currentNeed'),
        timeline: v('timeline'),
        message: v('message'),
        consent: (form.querySelector('#wl-consent') as HTMLInputElement).checked,
        utm: getUtm(),
        submittedAt: new Date().toISOString(),
      };
    };

    // form_start: first interaction with any field, once per page view.
    let started = false;
    form.addEventListener('focusin', () => {
      if (started) return;
      started = true;
      trackEvent('form_start', { language: form.dataset.language });
    });

    // Clear a field's error as soon as the user fixes it.
    form.addEventListener('input', (e) => {
      const el = e.target as HTMLElement;
      if (el.getAttribute('aria-invalid') === 'true') showFieldError(el, false);
    });

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      errorBox.hidden = true;
      trackEvent('form_submit_attempt', { language: form.dataset.language });
      if (!validate()) return;

      submitBtn.disabled = true;
      submitBtn.textContent = submitBtn.dataset.labelBusy ?? '…';

      const payload = buildPayload();
      // Non-PII qualification data only — safe for analytics.
      const eventProps = {
        ctaSource: payload.ctaSource,
        organizationType: payload.organizationType,
        currentNeed: payload.currentNeed,
        timeline: payload.timeline,
        language: payload.language,
      };

      try {
        if (isLive) {
          const res = await fetch(endpoint, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json', Accept: 'application/json' },
            body: JSON.stringify(payload),
          });
          if (!res.ok) throw new Error(`Lead endpoint responded ${res.status}`);
        } else {
          // DEMO MODE — nothing is sent anywhere.
          console.info('[leadCapture] demo mode — payload NOT sent:', payload);
          await new Promise((r) => setTimeout(r, 500));
        }

        trackEvent('form_submit_success', eventProps);
        form.hidden = true;
        successBox.hidden = false;
        successBox.scrollIntoView({ behavior: 'smooth', block: 'center' });
        successBox.focus({ preventScroll: true });
      } catch (err) {
        console.error('[leadCapture] submission failed:', err);
        trackEvent('form_submit_error', eventProps);
        errorBox.hidden = false;
      } finally {
        submitBtn.disabled = false;
        submitBtn.textContent = submitBtn.dataset.label ?? '';
      }
    });
  }
</script>
