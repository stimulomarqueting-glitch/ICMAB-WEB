// The site keeps its copy as typed TypeScript modules under src/content/{en,es},
// not as Markdown collections. Declaring no collections stops Astro from
// treating those folders as legacy collections at build time.
export const collections = {};
